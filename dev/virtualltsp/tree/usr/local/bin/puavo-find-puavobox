#!/bin/sh

ANS="$(avahi-browse -artpk| grep "=;.*;IPv4;puavo.*bootserver" | cut -d";" -f8|head -n1)"

if test -n "$ANS"; then
    if test "$ANS" = "10.249.15.254";then
	exit 1
    fi    
    echo "$ANS"
    exit 0
else
    exit 2
fi


