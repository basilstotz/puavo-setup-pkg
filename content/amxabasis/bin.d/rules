#!/bin/sh

set -eu

command=$1
upstream_dir=$2
cd "$upstream_dir"

case "${command}" in
  configure)
      for B in ./bin/*.sh; do
	  if test -x $B; then
	      echo "info:   running $(basename $B)"
	      $B
	  fi
      done
    ;;
	
  unconfigure)

    ;;
    
  *)
    ;;
esac
